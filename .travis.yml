addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - ubuntu-sdk-team
    packages:
    - qtcreator
    - qtcreator-dev

language: cpp
sudo: false
cache:
    apt: true
    ccache: true
    directories:
        deps

compiler:
  - clang
  - g++

before_install:
  - cd ./deps
  - ls
  - git clone https://github.com/g-truc/glm.git
  - cd ./glm
  - cmake . -D CMAKE_INSTALL_PREFIX:FILEPATH=deps

script:
  - qmake ./BALLS/BALLS.pro
  - make